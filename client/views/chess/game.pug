extends ../layouts/main.pug

block styles
    link(rel="stylesheet", href=h.asset("variable.css"))
    link(rel="stylesheet", href=h.asset("chess/game.css"))

block script
    script(type="module", src="/socket.io/socket.io.js")
    script(type="module", src=h.asset("chess/resize.js")) 
    script(type="module", src=h.asset("services/chess/board.js")) 
    script(type="module", src=h.asset("services/socket.io/game.js")) 

block body
    input(type="hidden", name="gameId", value=gameId)
    cg-container
        cg-board 
        

